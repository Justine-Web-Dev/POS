{
  "name": "pos-system-root",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "Client",
    "server"
  ],
  "scripts": {
    "build": "npm run build --workspace=Client"
  },
  "engines": {
    "node": "22.x"
  }
}
